<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_ROOT}/application-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- each archived file's size will be max 10MB -->
            <maxFileSize>10MB</maxFileSize>
            <!-- 30 days to keep -->
            <maxHistory>30</maxHistory>
            <!-- total size of all archive files, if total size > 100GB, it will delete old archived file -->
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} %msg%n</pattern>
        </encoder>
        <!--            <file>/Users/kushal/Development/nocode-work/studio-backend/application.log</file>-->
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

    <springProfile name="default">
        <logger name="org.apache.kafka.clients.consumer.internals.ConsumerCoordinator" level="WARN"/>
        <logger name="org.springframework" level="INFO"/>
        <logger name="org.okapi" level="INFO"/>
        <logger name="org.apache" level="INFO"/>
        <logger name="com.amazonaws" level="INFO"/>
    </springProfile>

    <springProfile name="integration-test">
        <logger name="org.springframework" level="INFO"/>
        <logger name="org.apache" level="INFO"/>
        <logger name="com.amazonaws" level="INFO"/>
    </springProfile>

</configuration>
