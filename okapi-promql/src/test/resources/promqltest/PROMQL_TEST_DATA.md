PromQL testdata is synced from the Prometheus upstream repository.

Run `scripts/update-promqltestdata.sh` from the repo root to refresh
`okapi-promql/src/test/resources/promqltest/testdata` and update the
`.upstream-commit` stamp.
