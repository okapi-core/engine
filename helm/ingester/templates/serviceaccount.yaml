{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ingester.fullname" . }}
  labels:
    {{- include "ingester.labels" . | nindent 4 }}
{{- end }}
